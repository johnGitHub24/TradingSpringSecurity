# TradingSpringSecurity 驗證設計

> 衝突以 [規格書.md](../規格書.md) 第 4／5 章為準。  
> 規範：EngineeringOS `knowledge/validation-design.md`

## 1. 四層驗證

| 層 | 機制 | 典型失敗 |
|----|------|----------|
| L1 傳輸／認證 | JWT Bearer、`SecurityFilterChain`、`hasRole("ADMIN")` on DELETE | 401／403 |
| L2 格式 | DTO `@Valid` | 400 `VALIDATION_FAILED` |
| L3 業務 | Service（冪等、不存在、帳號重複） | 404／409 |
| L4 資料 | DB UNIQUE／NOT NULL | 應少見（L3 先攔） |

## 2. 認證流程

```text
POST /api/auth/register → 201
POST /api/auth/login → 200 { token, tokenType, username, roles }
後續：Authorization: Bearer <token>
  → JwtAuthenticationFilter → SecurityContext
```

密碼：BCrypt。Token：HS256（`app.jwt.secret`、`expiration-ms`）。Session：STATELESS。

## 3. 權限矩陣

| 端點 | 公開 | USER | ADMIN |
|------|------|------|-------|
| POST `/api/auth/register`、`/login` | ✅ | — | — |
| Swagger／health／h2-console | ✅ | — | — |
| POST／GET／PATCH `/api/v1/orders/**` | — | ✅ | ✅ |
| DELETE `/api/v1/orders/{id}` | — | — | ✅ |

來源：`SecurityConfig` — `DELETE /api/v1/orders/**` → `hasRole("ADMIN")`；其餘需 `authenticated()`。

## 4. 錯誤碼

| 情境 | HTTP | errorCode |
|------|------|-----------|
| 欄位驗證失敗 | 400 | `VALIDATION_FAILED` |
| 帳密錯誤／未帶 token | 401 | `INVALID_CREDENTIALS`／— |
| 非 ADMIN 刪除 | 403 | — |
| 訂單不存在 | 404 | `ORDER_NOT_FOUND` |
| 冪等重複／帳號已存在 | 409 | `DUPLICATE_ORDER`／`USERNAME_EXISTS` |

## 5. 對照程式

- Handler：`GlobalExceptionHandler`
- Filter：`JwtAuthenticationFilter`、`JwtTokenProvider`
- Config：`SecurityConfig`
- DTO：`CreateOrderRequest`、`LoginRequest`、`RegisterRequest`
